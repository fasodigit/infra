// SPDX-License-Identifier: AGPL-3.0-or-later
//! Upstream mTLS — SPIFFE-authenticated TLS to the backend.
//!
//! **M0 scaffolding**: empty stub.  M2 wires `armageddon-mesh` here so that
//! the outbound connection carries the gateway's SVID and validates the
//! peer SPIFFE ID against the trust domain allow-list.

// Intentionally empty — reserved for M2.
