{{- /* SPDX-License-Identifier: AGPL-3.0-or-later */ -}}
{{- if .Values.networkPolicy.enabled }}
# Default deny all ingress/egress
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "notifier-ms.fullname" . }}-deny-all
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "notifier-ms.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "notifier-ms.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
---
# Allow ingress from ARMAGEDDON gateway on HTTP (8803)
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "notifier-ms.fullname" . }}-allow-ingress
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "notifier-ms.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "notifier-ms.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: armageddon
        - namespaceSelector:
            matchLabels:
              faso.gov.bf/tier: gateway
      ports:
        - protocol: TCP
          port: 8803
---
# Allow Prometheus scrape on actuator port 9003
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "notifier-ms.fullname" . }}-allow-metrics
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "notifier-ms.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "notifier-ms.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: observability
          podSelector:
            matchLabels:
              app.kubernetes.io/name: prometheus
      ports:
        - protocol: TCP
          port: 9003
---
# Allow egress to postgres, kaya, redpanda, smtp, vault, consul, DNS
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "notifier-ms.fullname" . }}-allow-egress
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "notifier-ms.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "notifier-ms.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Egress
  egress:
    # PostgreSQL
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: postgres
      ports:
        - protocol: TCP
          port: 5432
    # KAYA (RESP3)
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kaya
      ports:
        - protocol: TCP
          port: 6380
    # Redpanda (Kafka protocol)
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: redpanda
      ports:
        - protocol: TCP
          port: 9092
    # SMTP (outbound email)
    - ports:
        - protocol: TCP
          port: 587
    # Vault
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: vault
      ports:
        - protocol: TCP
          port: 8200
    # Consul
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: consul
      ports:
        - protocol: TCP
          port: 8500
    # DNS (CoreDNS)
    - ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
{{- end }}
